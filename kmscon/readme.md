# KMSCON config

- There are 2 ways to use it:
    1. 'sudo kmscon'
    2. enable systemctl service.

- Ensure the font is installed in a system-wide path, like:
/usr/share/fonts/

- Ensure the font is a Mono font. Otherwise, icons won't display properly.
    recommended font: FiraCode Nerd Font Mono

